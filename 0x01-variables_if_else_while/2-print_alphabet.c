#include <stdio.h>

/**
 * main - Entry Point.
 *
 * Description: printing alphabet in lower case.
 *
 * Return: Always 0 (Success).
 *
*/

int main(void)
{	
	for (char ch= 'a'; a <= 'z', ch++)
	putchar(ch);
	return (0);
}	
